const go = async () => {
    const sigShare = await LitActions.signEcdsa({
        messageRequested, //param
        publicKey,  //param
        sigName: "sig1"
    });
    
    const url = "";
    const result = await fetch(url, {
        
    }

    httts://hackathon.fvm.com/checkMeta/${cid}&sigShare=${sigShare}&publicKey=${publicKey}
    

    


}

go();