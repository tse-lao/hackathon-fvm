// src/wallets/walletConnectors/rabbyWallet/rabbyWallet.ts
import { InjectedConnector } from "wagmi/connectors/injected";
var rabbyWallet = ({
  chains,
  ...options
}) => ({
  id: "rabby",
  name: "Rabby",
  iconUrl: async () => (await import("./rabbyWallet-ULD7CWVP.js")).default,
  iconBackground: "#fff",
  installed: typeof window !== "undefined" && typeof window.ethereum !== "undefined" && window.ethereum.isRabby === true,
  downloadUrls: {
    browserExtension: "https://chrome.google.com/webstore/detail/rabby/acmacodkjbdgmoleebolmdjonilkdbch"
  },
  createConnector: () => ({
    connector: new InjectedConnector({
      chains,
      options
    })
  })
});

export {
  rabbyWallet
};
